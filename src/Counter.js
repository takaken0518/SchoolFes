import React from 'react'

const votes = 0
const test = 'this is TEST'

const Counter = (test) => {
  return (
    <div>
    Counter
    
    </div>
  )
}

console.log(Counter)

export default Counter